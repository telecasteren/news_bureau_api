## TO DO

- ask teacher about JSDoc amount and redundancy
- Test all endpoints with Postman
  - zod validation and error handling: ✅
    ARTICLES:
  - POST /articles create an article ✅
  - PATCH /articles/:id updates an article ✅
  - GET /articles returns list of all articles ✅
  - GET /articles/:id returns a single article matching the id ✅
    AUTH:
  - POST /auth/register creates a user ✅
  - POST /auth/login logs in user and returns token ✅
    USERS:
  - PATCH /users/:id updates a user (user 18) ✅
  - GET /users returns list of all users ✅
  - GET /users/:id returns a single user matching the id ✅
